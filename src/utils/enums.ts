export enum OrderStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
}

export enum ShipmentStatus {
  PENDING = 'pending',
  IN_TRANSIT = 'in_transit',
  DELIVERED = 'delivered',
}

export enum UserRole {
  CUSTOMER = 'customer',
  ADMIN = 'admin',
  COURRIER = 'courrier',
}
